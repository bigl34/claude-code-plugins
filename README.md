# Claude Code Plugins

A Claude Code plugin for claude code plugins operations.

## Setup

1. Install this plugin in Claude Code
2. Copy `config.template.json` to `config.json` and fill in your credentials
3. Run `cd scripts && npm install` to install dependencies

## License

MIT
